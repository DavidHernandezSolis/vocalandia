import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.languagetherapy.vocalandia',
  appName: 'vocalandia',
  webDir: 'www'
};

export default config;
